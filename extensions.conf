exten => s,1,Answer()
 same => n,Playback(welcome)          ; Eine Begrüßungsnachricht abspielen
 same => n,Background(menu-options)   ; Menüoptionen abspielen und auf Benutzereingabe warten
 same => n,WaitExten()